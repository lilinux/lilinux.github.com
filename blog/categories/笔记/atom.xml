<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: 笔记 | lilinux's blog]]></title>
  <link href="http://lilinux.github.com/blog/categories/笔记/atom.xml" rel="self"/>
  <link href="http://lilinux.github.com/"/>
  <updated>2013-05-08T18:05:54+08:00</updated>
  <id>http://lilinux.github.com/</id>
  <author>
    <name><![CDATA[lilinux]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[clint代码阅读笔记]]></title>
    <link href="http://lilinux.github.com/blog/2012/07/clintdai-ma-yue-du-bi-ji-1/"/>
    <updated>2012-07-25T17:18:00+08:00</updated>
    <id>http://lilinux.github.com/blog/2012/07/clintdai-ma-yue-du-bi-ji-1</id>
    <content type="html"><![CDATA[<p>一直认为自己的水平还有待提高，所以尝试在<a href="http://www.github.com">github</a>上找一些有意思的开源代码阅读，即可以陶冶情操，又可以学到一些在实践中各种闪光的东西，如果能参与进去那就更好了。</p>

<p>最先看的是<code>clint</code>, 原托管地址<a href="https://github.com/kennethreitz/clint">在此</a></p>

<!--more-->


<p>clint是一个命令行程序的辅助库，它做了一些繁琐的事，截一断介绍</p>

<p>```
C ommand L ine IN terface T ools .</p>

<p>Clint is awesome. Crazy awesome. It supports colors, but detects if the session is a TTY, so doesn't render the colors if you're piping stuff around. Automagically.</p>

<p>Awesome nest-able indentation context manager. Example: (with indent(4): puts('indented text')). It supports custom email-style quotes. Of course, it supports color too, if and when needed.</p>

<p>It has an awesome Column printer with optional auto-expanding columns. It detects how wide your current console is and adjusts accordingly. It wraps your words properly to fit the column size. With or without colors mixed in. All with a single function call.</p>

<p>The world's easiest to use implicit argument system w/ chaining methods for filtering. Seriously.</p>

<p>```</p>

<p>确实看到了一些亮点，做笔记如下：</p>

<h2>获取管道传进来的数据</h2>

<p>管道或重定向将数据发送到当前进程的标准输入。首先需要确定标准输入是否连接了管道然后再读出。</p>

<p>```python piped_in
if sys.stdin.isatty():</p>

<pre><code>return sys.stdin.read()
</code></pre>

<p>else:</p>

<pre><code>return None
</code></pre>

<p>```
原理很简单，但不容易想到</p>

<h2>progressbar</h2>

<p>在命令行下的进度条。原来在一些安装或检测脚本的执行结果中看到，进度条会在更新时覆盖掉原有的状态，原来一直以为是和终端相关的很帅气的功能，看到代码原来是很少见的回车符<code>\r</code>，它只回到行首而不换行。</p>

<p>```python update state in this line
for i in range(100, 0, -1):</p>

<pre><code>sys.stderr.write('time left: %3d\r'%i)
time.sleep(1)
</code></pre>

<p>```</p>

<h2>性能优化</h2>

<p>发现clint的代码中对list的处理不够优雅，有些代码可以改得更快更简单。</p>

<p>比如<code>tsplit</code>函数和<code>schunk</code>函数</p>

<p>```python tsplit
def tsplit(string, delimiters):</p>

<pre><code>"""Behaves str.split but supports tuples of delimiters."""

delimiters = tuple(delimiters)
stack = [string,]

for delimiter in delimiters:
    for i, substring in enumerate(stack):
        substack = substring.split(delimiter)
        stack.pop(i)
        for j, _substring in enumerate(substack):
            stack.insert(i+j, _substring)

return stack
</code></pre>

<p>```</p>

<p>```python schunk
def schunk(string, size):</p>

<pre><code>"""Splits string into n sized chunks."""

stack = []

substack = []
current_count = 0

for char in string:
    if not current_count &lt; size:
        stack.append(''.join(substack))
        substack = []
        current_count = 0

    substack.append(char)
    current_count += 1

if len(substack):
    stack.append(''.join(substack))

return stack
</code></pre>

<p>```</p>

<p>这两段代码具有明显的C程序员风格，而我一直认为<strong>Python代码的好坏可以从缩进层次上看出</strong>，如果缩进得扭扭捏捏，那多半是可以优化的，<strong>尤其是对列表的处理</strong></p>

<p>我尝试优化这两个函数</p>

<p>```python tsplit2
def tsplit2(string, delimiters):</p>

<pre><code>delimiters = tuple(delimiters)
if len(delimiters) &lt; 1:
    return [string,]
final_delimiter = delimiters[0]
for i in delimiters[1:]:
    string = string.replace(i, final_delimiter)
return string.split(final_delimiter)
</code></pre>

<p>```</p>

<p>```python schunk2
def schunk2(string, size):</p>

<pre><code>return [string[i:i+size] for i in range(0, len(string), size)]
</code></pre>

<p>```</p>

<p>不管是从缩进上还是从行数上，是不是都要漂亮一些呢。</p>

<p>好，我们来做一下性能对比：</p>

<p>```python tsplit性能对比
In [35]: timeit -n 10000 utils.tsplit(s, ('abc', 'def', 'for', 'xx', 'oo', ' '))<br/>
10000 loops, best of 3: 1.62 ms per loop</p>

<p>In [36]: timeit -n 10000 utils.tsplit2(s, ('abc', 'def', 'for', 'xx', 'oo', ' '))
10000 loops, best of 3: 101 us per loop
```</p>

<p>```python schunk性能对比
In [27]: timeit -n 10000 utils.schunk2(s, 8)
10000 loops, best of 3: 66.3 us per loop</p>

<p>In [28]: timeit -n 10000 utils.schunk(s, 8)
10000 loops, best of 3: 956 us per loop
```</p>

<p>竟然都有近10倍以上的性能提升，当然这个比值并不一定准备，我只是随便找了个参数测试。</p>

<p>其实这也印证了另外一条Python和C/C++不同的结论：</p>

<p><strong>要尽量使用Python的标准库，自己写的代码即使再优化也难以达到和库函数媲美的效率</strong></p>

<h2>The End</h2>

<p>clint的代码很朴实有力，通过亮点的叠加实现了一个非常有用的扩展库。以后写对外的命令行程序一定要考虑使用它。</p>

<p>关于最后那个性能的改良，也许有点画蛇添足，就没有去pull代码了，只发了个issue看开发者怎么说。</p>
]]></content>
  </entry>
  
</feed>
